menu "Options"

config LIBPDRAW_PYTHON_PYTHON3
  bool "Compile libpdraw_python for python3"
  default n
  help
    If set, the binding will be compatible with python3 only.
    If not set (default), the binding will be compatible with python2 only.

endmenu
